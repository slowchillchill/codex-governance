# Codex 治理仓库规则

## 治理声明

- 只有用户在当前消息中显式调用 `codex-stage-orchestrator`，并给出明确开发需求时，本仓库才启用
  [工程准则](governance/engineering-principles.md)中的 GitHub 任务治理。
- 未显式调用时不启用本治理及其 GitHub 闭环；用户明确禁止使用时不启用。仓库接入声明、启动
  `--profile codex-governance` 或仅提及名称均不能替代显式调用；普通任务按用户授权直接处理，
  不因本治理创建 Issue、派生治理角色或执行 GitHub 闭环。
- 同一已显式授权任务内的澄清、状态查询和继续执行沿用该任务授权；新任务或下一阶段必须重新显式调用。
- 用户当前明确指令决定目标与授权；启用治理后，对应 Issue 是任务合同，PR 保存差异与证据，正式
  Review 保存审查结论，合并提交保存最终结果。

## 仓库边界

- 本仓库保存公共治理规则、技能和配置示例。本机配置及认证材料保留在仓库外。
- 不因安装或修改本治理，改动其他治理工具的配置、服务或认证材料。
- 不创建任务型 `demand/`、`plan/`、`changelog/`、本地游标或过程状态文件。

## 修改原则

- 保持治理正文、角色 TOML 示例、运行 Profile 示例、Skill 和接入模板语义一致。
- 公开内容使用仓库相对路径；本机绑定及工作树绝对路径只用于私有配置和角色交接，详见治理规范第三节。
- 只做可追溯到当前任务合同的最小修改，不顺手调整相邻规则。
- 不读取、显示、复制或提交令牌、私钥、口令、`.env` 正文或其他认证材料。
- 通用工程原则读取并遵守 [工程准则](governance/engineering-principles.md)；本仓库闭环权责以
  [治理规范](governance/codex-development-governance.md) 为准。治理变更按实际差异、引用和职责进行审查。

## 本仓库的多角色闭环

- 本仓库明确声明：采用 Codex GitHub 多角色闭环时，角色独立性本身构成顺序派生
  `scope-planner -> implementer -> reviewer` 的理由，不要求三个角色之间存在并行收益。
- 同一共享写入路径始终只有 `implementer` 一个写入者；`scope-planner` 与 `reviewer` 保持只读。
- `orchestrator` 只在当前 PR 精确头提交获得正式批准且不存在阻塞反馈时合并。
- Profile 默认以 `danger-full-access` 与 `never` 提供等价 YOLO 的命令执行权限，三个子代理使用相同
  默认值；父会话传给子代理的实时权限覆盖只改变命令执行权限，不扩大角色职责、GitHub 身份、任务
  范围或破坏性操作授权，也不解除管理员绕过禁令。
- 每次只处理用户当前明确需求对应的一个普通任务或阶段 Issue。准确 Issue 由 `orchestrator` 在当前
  仓库定位唯一匹配的现有对象，或在没有匹配对象时创建最小必要 Issue；下游角色不得自行发现任务。
  当前 Issue 完成后更新适用的上层 Issue 并停止，不创建、选择、激活或签发下一阶段。
